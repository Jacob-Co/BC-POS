{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "NODE_ENV=test mocha",
    "dev": "nodemon app.js",
    "start": "node app.js",
    "reinit": "cd ../.. && rm -rf barcode-backend-deploy && mkdir barcode-backend-deploy && cd barcode-backend-deploy && git init && heroku git:remote -a barcode-scanner-ay && git pull heroku master",
    "deploy": "cp -r ./ ../../barcode-backend-deploy && cd ../../barcode-backend-deploy && heroku git:remote -a barcode-scanner-ay && git add . && git commit -m 'deploy' && git push heroku master",
    "deploy-build-packs": "cd ../../barcode-backend-deploy/backend && heroku buildpacks:add jontewks/puppeteer",
    "check-hours": "heroku ps -a barcode-scanner-ay",
    "check-logs": "heroku logs -a barcode-scanner-ay"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "google-auth-library": "^7.0.4",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.12.5",
    "puppeteer": "^9.1.1"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "chai-http": "^4.3.0",
    "mocha": "^9.0.1"
  }
}
